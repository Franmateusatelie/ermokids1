name: ermokids
description: ErmoKids - Plataforma Educacional Infantil
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 🎵 Músicas
  audioplayers: ^6.5.1

  # ⭐ Progresso, estrelas, dados locais
  shared_preferences: ^2.5.4

  # 🔔 Notificações (rotinas dos pais)
  flutter_local_notifications: ^17.1.2

  # 🍎 Ícones iOS (padrão Flutter)
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/music/

